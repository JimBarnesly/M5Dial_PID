This build clears legacy app-managed Wi-Fi credentials and relies on WiFiManager/ESP32 SDK credential storage only.
If the device still fails after flashing, use Reset WiFi once and re-enter credentials.
